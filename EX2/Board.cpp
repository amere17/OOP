#include "Board.h"


/*
Board::Board()
{
}

void Board::print(std::vector<std::string> map, int size)
{
	for (size_t i = 0; i < size; i++)
		cout << map[i] << '\n';
}

void Board::draw(char sign, Position pos)
{
	map[pos.row][pos.col] = sign;
}

char Board::inPos(Position pos)
{
	return m_board[pos.row][pos.col];
}

void Board::loadMap(std::vector<std::string> &map, Player & robot, 
	vector<Guard>& guards,	Door & door, vector<Rock>& rocks, vector<Wall>& walls)
{
	
}
*/